# AppShell

Responsibilities:
- App bootstrap.
- Window lifecycle.
- Root navigation between main screens.

Implemented:
- `NativeClientBootstrap` to wire:
  - settings load/save
  - REST client creation
  - socket client factory
