"""Convenience entrypoint for Week-1 local dev.

Run:
  python main.py

This simply calls server.run(), but keeps the command consistent with the project roadmap.
"""

from server import run


if __name__ == "__main__":
    run()
